classdef Brew
    % Collate brew-level data

    methods(Static=true)
        
        % Create a build folder with signatures and metadata
        build(varargin);
                
    end % Static methods block
    
end