classdef MarkerSelection
% Algorithms for marker selection based on two-class comparisons

    methods (Static=true)        
       res = runAnalysis(varargin);
       saveResult(res, outpath, varargin);
    end % methods block

end
