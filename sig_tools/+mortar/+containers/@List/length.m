function len = length(obj)
%LENGTH Length of a list.
%   N = LENGTH(A) returns the number of elements in the list A.  LENGTH
%   is equivalent to SIZE(A,1).
%  
%   See also DATASET/SIZE.

len = obj.length;